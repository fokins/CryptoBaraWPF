<Page x:Class="WpfApp1.BalancePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1"
      mc:Ignorable="d" 
      MinHeight="600" MinWidth="900"
      Height="Auto" Width="Auto"
      Title="BalancePage">

    <Grid Background="#eeeeee">

        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Binance balance" FontSize="30" FontFamily="Segoe UI Semilight" Margin="10, 5, 0, 0"/>

        <Border Grid.Row="1" Background="White" Width="{Binding Path=ActualWidth, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=UIElement}}" HorizontalAlignment="Left" VerticalAlignment="Bottom" CornerRadius="20" Padding="0, 20, 0, 0">
            <ScrollViewer VerticalScrollBarVisibility="Visible" Height="{Binding Path=ActualHeight, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=UIElement}}">
                <StackPanel x:Name="BalanceStackPanel"/>
            </ScrollViewer>
        </Border>
    </Grid>
</Page>
